# Runtime dependencies
# =====================
# Python 3.9+ on Windows. tkinter ships with the standard CPython installer.

pywin32>=306            # Windows print spooler access (raw ZPL/TSPL passthrough)
pyserial>=3.5           # Serial port monitoring
pandas>=2.0.0           # CSV log handling and the box-label device table
qrcode>=7.4.0           # QR codes on box labels
reportlab>=4.0.0        # Box label PDF generation
Pillow>=10.0.0          # Image handling for PDF-based printing

# Optional: only needed for the PDF -> raster printing path (pdf_printer.py).
# Install one of these if you print existing PDF files instead of ZPL.
# PyMuPDF>=1.23.0
# PyPDF2>=3.0.0

# Optional: building the standalone executable (see build_gui_exe.py)
# pyinstaller>=6.0.0
